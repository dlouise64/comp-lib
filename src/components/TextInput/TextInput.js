import React from 'react';
// import PropTypes from 'prop-types';

import './TextInput.css';

class TextInput extends React.Component {
  render() {
    return (
      <div className="textInput">TextInput</div>
    );
  }
}

TextInput.propTypes = {
};

TextInput.defaultProps = {
}

export default TextInput;
